{% extends 'base.html.twig' %}

{% block title %}
  reinit mot de passe
{% endblock %}

{% block body %}


<div class="hero text-center  rounded-t-xl grow flex flex-col">
  <div class="w-[70%] mx-auto flex flex-col justify-center items-center ">
      <h1 class="text-slate-50 bg-slate-800 opacity-75 text-2xl sm:text-3xl md:text-4xl  font-bold p-5 my-8 rounded-xl " >Entrez dans la bibliothèque...</h1>
    
      
      {{ form_start(ResetPassword,{attr:{class:'flex flex-col mt-8 bg-slate-100 p-8 rounded'}}) }}
<h1 class="h3 mb-3 font-weight-normal">Entrez votre nouveau mot de passe ...</h1>

{{ form_label(ResetPassword.password,'Password',{'label_attr':{'class':'mt-4 mb-1 text-left',}}) }}
{{ form_widget(ResetPassword.password, {'attr':{'class':'px-2 py-1
ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-1 focus:ring-inset focus:ring-gray-600 sm:text-sm sm:leading-6'}}) }}
<span class="mb-4 text-left text-red-500">{{ form_errors(ResetPassword.password) }}</span>


<button type="submit" class="mt-5 btn-primary bg-slate-300 hover:bg-slate-200">Envoyer</button>
{{ form_end(ResetPassword) }}


  </div>

</div> 


{% endblock %}

